{
    "projects": [
        {
            "name": "backend",
            "root": "backend",
            "buildCommand": "npm run build",
            "outputDirectory": "dist",
            "devCommand": "npm run start"
        }
    ]
}
